{
  "name": "server",
  "version": "1.0.0",
  "description": "The SERVER API for Soshi Connect APP",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json && nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "jason2000-cpu"
  },
  "author": "jason2000-cpu",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^3.0.1",
    "uuid": "^9.0.1"
  }
}
